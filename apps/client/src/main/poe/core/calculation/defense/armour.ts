import type { CharacterSnapshot } from '../snapshot.model.js';
import type { ResolvedCharacterStats } from '../../stats/models/character-stats.model.js';

export function calculateArmourMitigation(stats: ResolvedCharacterStats, hitDamage: number): number {
  return 0;
}
